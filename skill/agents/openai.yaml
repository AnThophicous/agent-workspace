interface:
  display_name: "Paper Workspace"
  short_description: "Plan and preserve meaningful agent work"
  default_prompt: "Use $paper-workspace to create a Paper for this task, keep it current, and leave a checkpoint."
policy:
  allow_implicit_invocation: true
